CREATE TABLE IF NOT EXISTS CUSTOMERS (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CUSTOMER_NAME VARCHAR(255) NOT NULL
);

CREATE TABLE IF NOT EXISTS PURCHASES (
    ID INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CUSTOMER_ID BIGINT,
    PRODUCT VARCHAR(255),
    PURCHASE_DATE TIMESTAMP,
    FOREIGN KEY (CUSTOMER_ID) REFERENCES CUSTOMERS(ID)
);

INSERT INTO CUSTOMERS (CUSTOMER_NAME) VALUES ('Bruce');
INSERT INTO CUSTOMERS (CUSTOMER_NAME) VALUES ('Paul');
INSERT INTO CUSTOMERS (CUSTOMER_NAME) VALUES ('John');

INSERT INTO PURCHASES (CUSTOMER_ID, PRODUCT, PURCHASE_DATE) VALUES (1, 'Baseball', '2023-01-01');
INSERT INTO PURCHASES (CUSTOMER_ID, PRODUCT, PURCHASE_DATE) VALUES (2, 'Football', '2023-01-02');
INSERT INTO PURCHASES (CUSTOMER_ID, PRODUCT, PURCHASE_DATE) VALUES (3, 'Basketball', '2023-01-03');